import React from "react";
import classes from "./styles/OrderModal.module.css";

export default function OrderModal({downloadFunc, ...props}){
    return(
        <div>
            
        </div>
    )
}